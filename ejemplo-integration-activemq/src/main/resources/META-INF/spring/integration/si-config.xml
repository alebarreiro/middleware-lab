<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-jms="http://www.springframework.org/schema/integration/jms"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration
		http://www.springframework.org/schema/integration/spring-integration.xsd
        http://www.springframework.org/schema/integration/jms
        http://www.springframework.org/schema/integration/jms/spring-integration-jms.xsd"
>

    <int:channel id="jsonNewOrders"/>
    <int:channel id="unused"/>
    <bean id="xmlTransformer" class="com.middleware.XmlTransformer" />

    <!-- Crea la coneccion con activeMQ, importante que el id sea connectionFactory -->
    <bean id="connectionFactory" class="org.springframework.jms.connection.CachingConnectionFactory">
        <property name="targetConnectionFactory">
            <bean class="org.apache.activemq.ActiveMQConnectionFactory">
                <property name="brokerURL" value="tcp://mbpro-toni.local:61616"/>
            </bean>
        </property>
        <property name="sessionCacheSize" value="10"/>
        <property name="cacheProducers" value="false"/>
    </bean>

    <!-- crea la cola orders en activeMQ -->
    <bean id="cafeOrdersQueue" class="org.apache.activemq.command.ActiveMQQueue">
        <constructor-arg value="orders"/>
    </bean>

    <!-- lee de la cola de activeMQ y deposita en canal -->
    <int-jms:inbound-channel-adapter id="newOrdersInChannelAdapter" destination="cafeOrdersQueue" channel="jsonNewOrders">
        <int:poller fixed-rate="10000" />
    </int-jms:inbound-channel-adapter>

    <!-- transforma xml a pojo orden -->
    <int:transformer ref="xmlTransformer" output-channel="unused"
                     input-channel="jsonNewOrders" method="trasform">
    </int:transformer>
</beans>